name: Cache Workflow

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: prime-cache-1d18359
        uses: actions/cache@v4
        with:
          path: ~/.cache/mydeps
          key: cache-1d18359
